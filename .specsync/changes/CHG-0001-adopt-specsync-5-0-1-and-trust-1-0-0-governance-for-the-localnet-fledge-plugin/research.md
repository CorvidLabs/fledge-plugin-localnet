---
change: CHG-0001-adopt-specsync-5-0-1-and-trust-1-0-0-governance-for-the-localnet-fledge-plugin
artifact: research
---

# Research

The self-contained harness supplies protocol responses without requiring Docker or AlgoKit and covers 13 running, stopped, validation, protocol-version, reset, account, and command cases.

SpecSync measures the hook but not the extensionless primary executable, so the threshold is explicitly 0 and the native syntax/protocol lane is blocking. ShellCheck currently reports unrelated pre-existing findings and is not part of existing CI, so those repairs are isolated from this migration.
